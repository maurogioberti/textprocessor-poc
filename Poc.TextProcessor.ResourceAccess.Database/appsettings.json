{
  // Connection string settings are utilized exclusively by the EF Designer for database setup and migration execution.
  "ConnectionStrings": {
    "SqlServerConnection": "Server=localhost;Database=PocTextProcessor;Trusted_Connection=True;TrustServerCertificate=True",
    "SqliteConnection": "SQLite connection string"
  },
  "DatabaseProvider": "SqlServer" // Change to "Sqlite" to use SQLite
}